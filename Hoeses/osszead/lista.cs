class Lista
{
    const int length = 80;
    static Random rnd = new();
    static List<int> fromleft = new List<int>();
    static List<int> fromright = [];
    static void NewNumber (double prob){
        if (rnd.NextDouble() < prob)
        {
            
        }
    }
    static void Main(){
        Console.SetWindowSize(85,10);
        
    }
}